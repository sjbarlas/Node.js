setInterval(function() {

	console.log('beef');

}, 2000); // every 2 seconds, any code that is in the call back will be called

// Timeout runs once after a period of interval
// Intervals runs repeatedly
